/**
 * Shared plugin index committed with the repository.
 *
 * Keep this list empty by default. Personal plugins should go into:
 * - plugins/page-text-extraction/index.local.js
 */
export const pageTextExtractionPlugins = [];

export default pageTextExtractionPlugins;
